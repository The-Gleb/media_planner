import type { ActivePlan } from '../../domain/planning'
import type { ExecutionMode } from '../../domain/types'
const EXECUTION={adaptive_max:'Максимизация KPI',adaptive:'Удержание плана',frozen:'Замороженный план'} as const
const KPI={unique_reach:'Охват (unique_reach)',clicks:'Клики (clicks)',conversions:'Конверсии (conversions)'}
export function PlanSummary({plan,execution,historyCount}:{plan:ActivePlan;execution?:ExecutionMode;historyCount?:number}){return <section className="card" aria-labelledby="plan-title"><div className="status-line"><h2 id="plan-title">Медиаплан</h2><span className="status-badge" data-tone="ok">Готов</span></div><dl className="summary-grid"><Item label="ID плана" value={plan.planId.slice(0,12)+'…'}/><Item label="Режим" value={plan.type==='target_kpi'?'Целевой KPI':'Фиксированный бюджет'}/><Item label={plan.type==='target_kpi'?'Расчётный бюджет':'Бюджет'} value={plan.budget+' '+plan.currency}/><Item label="Резерв вне каналов" value={plan.unallocatedBudget+' '+plan.currency}/><Item label="Горизонт" value={'['+plan.horizon.fromHour+', '+plan.horizon.toHour+') · '+(plan.horizon.toHour-plan.horizon.fromHour)+' ч'}/><Item label="Аллокации" value={String(plan.allocations.length)}/><Item label="KPI" value={KPI[plan.optimize]}/><Item label="Стратегия" value={plan.strategy==='uniform'?'Равномерно (uniform)':'Оптимизированная по каталогу'}/>{plan.target&&<Item label="Цель" value={`${plan.target.value} · ${KPI[plan.target.metric]}`}/>}{execution&&<Item label="Исполнение" value={EXECUTION[execution]}/>}<Item label="Источник знаний" value={historyCount?`каталог + ${historyCount} прошл. камп.`:'публичный каталог'}/></dl>{plan.unallocatedBudget!=='0.000000'&&<p className="forecast-unavailable"><strong>Часть бюджета оставлена в резерве:</strong> benchmark не видит положительной предельной отдачи после насыщения доступного инвентаря.</p>}{plan.expected?<><p className="forecast-unavailable"><strong>Прогноз плана:</strong> {historyCount?`по фактам ${historyCount} прошлых кампаний на этом рынке поверх публичного каталога`:'по публичным диапазонам каталога'}, без знания скрытого seed мира.</p><dl className="summary-grid"><Item label="Показы" value={plan.expected.impressions}/><Item label="Охват без дедупликации" value={plan.expected.uniqueReach}/><Item label="Клики" value={plan.expected.clicks}/><Item label="Конверсии" value={plan.expected.conversions}/><Item label="Расход" value={plan.expected.spend+' '+plan.currency}/></dl></>:<p className="forecast-unavailable"><strong>Прогноз не отображается.</strong> План содержит точные бюджетные лимиты; ожидаемые показатели не подменяются нулями.</p>}</section>}
function Item({label,value}:{label:string;value:string}){return <div className="summary-item"><dt>{label}</dt><dd>{value}</dd></div>}
