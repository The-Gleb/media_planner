import { render,screen } from '@testing-library/react';import { describe,expect,it } from 'vitest';import { AllocationTable } from './AllocationTable';import { activatePlan } from './activePlan';import type { MediaPlan } from '../../domain/planning';describe('maximum planning performance',()=>{it('indexes 43,200 allocations and mounts only one 24-hour page',()=>{const channels=Array.from({length:20},(_,i)=>'channel_'+String(i).padStart(2,'0')),allocations=[] as MediaPlan['allocations'];for(let hour=0;hour<2160;hour++)for(const channelId of channels)allocations.push({channelId,hour,budgetCap:'0.000000',expected:null});const plan:MediaPlan={requestId:'00000000-0000-4000-8000-000000000010',stateRevision:0,planId:'d'.repeat(64),feasible:true,type:'fixed_budget',strategy:'uniform',optimize:'clicks',currency:'RUB',budget:'0.000000',horizon:{fromHour:0,toHour:2160},expected:null,allocations,requiredBudget:null,reason:null,target:null},started=performance.now(),active=activatePlan(plan,plan.requestId,0,channels);expect(performance.now()-started).toBeLessThan(1500);render(<AllocationTable plan={active} currentHour={0}/>);expect(screen.getAllByRole('row')).toHaveLength(25);expect(screen.getByText('Страница 1 из 90')).toBeVisible()})})
