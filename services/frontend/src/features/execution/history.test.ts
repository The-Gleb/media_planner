import { describe,expect,it } from 'vitest';import { commitHourlyResult } from './history';import { facts,result,session } from '../../test/fixtures';const pending={stepId:'00000000-0000-4000-8000-000000000001',expectedHour:session.currentHour,actions:[],etag:session.etag,attempt:1};describe('history commit',()=>{it('atomically advances session, history and exact facts',()=>{const next=commitHourlyResult([],session,facts,pending,result(),null);expect(next.history).toHaveLength(1);expect(next.session.remainingHours).toBe(1);expect(next.facts).toMatchObject({stateRevision:1,uniqueReach:'80',clicks:'10',conversions:'2',spent:'20.000000'})});it('rejects duplicate commit',()=>{const first=commitHourlyResult([],session,facts,pending,result(),null);expect(()=>commitHourlyResult(first.history,session,facts,pending,result(),null)).toThrow('duplicate_hourly_commit')})})
