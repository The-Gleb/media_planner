import { render,screen } from '@testing-library/react';import userEvent from '@testing-library/user-event';import { describe,expect,it,vi } from 'vitest';import { PlanningFields } from './PlanningFields';import { initialDraft } from './validation';import { metadata } from '../../test/fixtures';describe('PlanningFields',()=>{it('exposes accessible total budget, KPI and both strategies',()=>{render(<PlanningFields value={initialDraft(metadata).campaign} currency="RUB" errors={{}} disabled={false} onChange={()=>{}}/>);expect(screen.getByLabelText('Общий бюджет, RUB')).toBeVisible();expect(screen.getByLabelText('Оптимизируемая метрика')).toBeVisible();const strategy=screen.getByLabelText('Стратегия');expect(strategy).toBeEnabled();expect(screen.getByRole('option',{name:/Равномерно/})).toBeVisible();expect(screen.getByRole('option',{name:/Оптимизированная/})).toBeVisible()});it('keeps target mode focusable, validates fields, and explains no mutation',async()=>{const change=vi.fn();const campaign={...initialDraft(metadata).campaign,planType:'target_kpi' as const,targetValue:'0'};render(<PlanningFields value={campaign} currency="RUB" errors={{'campaign.targetValue':'Введите положительное значение'}} disabled={false} onChange={change}/>);const mode=screen.getByRole('radio',{name:/Целевой KPI — пока недоступно/});mode.focus();expect(mode).toHaveFocus();expect(screen.getByRole('textbox', { name: /Значение цели/ })).toHaveAccessibleDescription('Введите положительное значение');expect(screen.getByRole('status')).toHaveTextContent(/не изменит Simulator/);await userEvent.click(screen.getByRole('radio',{name:'Фиксированный бюджет'}));expect(change).toHaveBeenCalled()})})
